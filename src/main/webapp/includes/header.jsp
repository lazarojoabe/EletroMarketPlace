<%@ page language="java" contentType="text/html; charset=UTF-8" pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <style>
        :root {
            --azul-eletro: #0077c7; --cor-fundo: #eef3f7; --cor-cartao: #ffffff;
            --cor-sucesso-padrao: #2ecc71; --cor-perigo-padrao: #e74c3c;
        }
        body { font-family: 'Roboto', sans-serif; background-color: var(--cor-fundo); display: flex; flex-direction: column; align-items: center; padding: 40px 20px; }
        .container { width: 95%; max-width: 750px; background-color: var(--cor-cartao); padding: 40px; border-radius: 16px; box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1); }
        h1 { color: var(--azul-eletro); text-align: center; text-transform: uppercase; }
    </style>